<nav class="header-nav">
    <div class="home">
        <h2>
            <a href="/">Agenda</a>
        </h2>
    </div>
    <div>
        <div class="nav-links">
            <% if(user) { %>
                <a class=" " href="/contato/index">Cadastrar novo contato</a>
            <% } %>
            <ul class="">
                <% if(user) { %>
                <li class="">
                    <a class="nav-link " href="/login/logout">Sair</a>
                </li>
                <% } else { %>
                <li class="">
                    <a class="nav-link " href="/">Home</a>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>